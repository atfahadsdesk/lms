# Used only by the local dev server proxy (vite.config.js)
VITE_API_PROXY_TARGET=http://localhost:5000

# Used in production builds: full URL to the deployed API's /api path.
# Leave unset for local dev (falls back to the proxy above).
# VITE_API_BASE_URL=https://your-server.onrender.com/api
